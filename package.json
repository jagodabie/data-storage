{
  "name": "follow-yourself",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "prepare": "husky install",
    "lint-client": "eslint 'client/'",
    "lint-server": "eslint 'server/'",
    "precommit": "npm run lint-client && npm run lint-server"
  },
  "devDependencies": {
    "husky": "^8.0.3"
  }
}
